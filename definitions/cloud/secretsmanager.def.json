{
  "typeId": "tech:cloud:secretsmanager",
  "label": "AWS Secrets Manager",
  "description": "Securely store, rotate, and retrieve secrets like database credentials and API keys",
  "category": "security",
  "style": {
    "shape": "rectangle",
    "color": "#DD344C",
    "icon": "key"
  },
  "constraints": {
    "maxOutputs": 10
  },
  "dataSchema": {
    "rotationEnabled": {
      "type": "boolean",
      "description": "Enable automatic secret rotation"
    },
    "kmsEncryption": {
      "type": "boolean",
      "default": true,
      "description": "Use AWS KMS for encryption at rest"
    }
  }
}
