version: '3.8'

services:
  # Backend Core
  core:
    build:
      context: .
      dockerfile: Dockerfile.core
    container_name: nodalis-core
    ports:
      - "3000:3000"
    volumes:
      - ./definitions:/app/definitions:ro
      - ./architecture.json:/app/architecture.json
      - ./.nodalis:/app/.nodalis
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    networks:
      - archi-network

  # Frontend Web
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    container_name: nodalis-web
    ports:
      - "8080:80"
    depends_on:
      - core
    restart: unless-stopped
    networks:
      - archi-network

networks:
  archi-network:
    driver: bridge
